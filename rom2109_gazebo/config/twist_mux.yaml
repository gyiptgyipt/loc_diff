twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic: db3_cmd_vel # rviz goal
        timeout: 0.5
        priority: 10
      joystick:
        topic: cmd_vel_joy # behavior tree
        timeout: 0.5
        priority: 100
